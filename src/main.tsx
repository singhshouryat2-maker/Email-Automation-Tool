import React from 'react'
import ReactDOM from 'react-dom/client'
import EmailAutomationTool from './EmailAutomationTool'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EmailAutomationTool />
  </React.StrictMode>,
)
